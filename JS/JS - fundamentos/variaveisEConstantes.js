// variaveis como o próprio nome já diz tem seu valor sempre varivel podendo mudar a todo instante;
let result = 10;


// constantes permanecerá com o seu valor a todo instante, muito importante ter cuidado nos casos das constantes pois no momento que for atribuido a constante não poderá ser mudada;
const MAX_VALUE = 100;


console.log(result);
console.log(MAX_VALUE);